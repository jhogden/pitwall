# Event Winner Car Images

Drop event-specific winner car images here.

Supported filenames:

- `<event-slug>.webp`
- `<event-slug>.jpg`
- `<event-slug>.png`

Example:

- `2025-australian-grand-prix.webp`

The event page will try these first. If no file exists, it falls back to the series car image.
